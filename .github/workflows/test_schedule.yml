name: 定时任务测试

on:
  push:
    branches: [main]
  schedule:
    - cron: '*/5 * * * *'  # 每5分钟测试一次
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: 测试
      run: echo "定时任务测试成功！$(date)"
